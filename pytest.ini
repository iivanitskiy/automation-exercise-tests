[pytest]
testpaths = tests
pythonpath = src
addopts = -v --tb=short --strict-markers
markers =
    smoke: critical path tests
    ui: browser UI tests
    api: REST API tests
    regression: full regression suite
